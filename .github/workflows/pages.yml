name: Публикация на GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

# Pages отдаёт проект по адресу /<repo>, поэтому сборка идёт с SITE_BASE.
# Сборка для гармаев.рф делается локально командой `npm run build` — она
# собирает тот же сайт с корня домена.
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: site/package-lock.json

      - name: Установить зависимости
        working-directory: site
        run: npm ci

      - name: Собрать сайт
        working-directory: site
        env:
          SITE_BASE: /${{ github.event.repository.name }}
          SITE_URL: https://${{ github.repository_owner }}.github.io
        run: npm run build

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
